### PROGETTO SCOLASTICO, DEV/TEST

Nella valutazione del tuo progetto finale guarderò in particolare la presenza e l'uso corretto delle seguenti tecniche e tecnologie:

Controllo e sanificazione input : Html5, JS, PHP (filter_var,...)
Front end responsive


Tecnica PostBack: unione della pagina html e dell'elaborazione php: uso $_SERVER['PHP_SELF'] o analoghi 
https://www.docsity.com/it/docs/linguaggio-php-il-postback/9458752/

Uso delle SESSIONI sia per mantenimento stato dell'applicazione sia per protezione delle pagine interne, Uso dei cookie, Uso delle prepared statement per interrogazione DB, Uso delle transazioni per l'accesso condiviso al DB, Uso delle  select ... LOCK IN SHARE MODE o FOR UPDATE per accesso condiviso, Uso di ajax o di websocket

DB: ER (tabelle e relazioni)

Tecnologie per invio email, pagamenti, ...

Certificazione a più fattori (php ed email, oauth,...)

Pattern M(odel)V(iew)C(ontroller)

Uso di API (Possibili API: sendgrid, postmarkapp per invio email, stripe per simulaz pagamenti, google maps per poi su mappa)
Ricordo che puoi proporti per la presentazione di una di queste voci "stile esami di maturità"

le informazion riguardo al mio progetto intanto sono:
N.,COGNOME,NOME,TITOLO PROGETTO,"Analisi, DB diagramma ER",PRIMA CONSEGNA PUNTUALE (% AI),FRONT END (tecnologie),BACK END (tecnologie),POSTBACK,SESSIONI,COOKIE,PREPARED STATEMENT,TRANSAZIONI su   DB,SELECT in share mode,AJAX/WEBSOCKET,CERTIFICAZIONE A PIU' FATTORI,MVC,API,LINK GITHUB,TECNOLOGIE AGGIUNTIVE
1,AGLIATI,FEDERICO,AgarthaRead,,,VUE | TailwindCss,Socket IO | NUXT 4,,,dati di accesso | preferenze dispositivo,,,,,,,google API per gli accesssi,,

e sono anche la tabella delle valutazioni (per questo alcune cose sono vuote)

questo programma lo sviluppo sul mio computer a casa e su github codespace a scuola (i computer hanno node non aggiornato)
node aggiornato mi serve per NUXT 4

il mio progetto consiste in un sito, simile a kindle unlimited, dove gli utenti possono registrari e leggere, comprare, salvare, commentare libri (che possono essere presi sia tramite un API che salvati su un database o un opzione migliore), fumetti e manga (cosi integro anche una parte per le immagini), giornali ...

linguaggi: HTML/VUE TailwindCss, JS/TP, SQL, 
tecnologie: NUXT 4, Socket IO

struttura:

 ||    ||    ||    ||    ||    ||    ||    ||    ||    ||   
 ||    ||    ||    ||    ||    ||    ||    ||    ||    ||   
\||/  \||/  \||/  \||/  \||/  \||/  \||/  \||/  \||/  \||/  
 \/    \/    \/    \/    \/    \/    \/    \/    \/    \/   
PRESA DA -> https://nuxt.com/docs/4.x/directory-structure
 /\    /\    /\    /\    /\    /\    /\    /\    /\    /\
/||\  /||\  /||\  /||\  /||\  /||\  /||\  /||\  /||\  /||\
 ||    ||    ||    ||    ||    ||    ||    ||    ||    ||  
 ||    ||    ||    ||    ||    ||    ||    ||    ||    ||  
 
AgarthaRead/
├─ apps/
│  ├─ web/                                  # Nuxt 4 (frontend + server routes)
│  │  ├─ app/
│  │  │  ├─ pages/
│  │  │  │  ├─ index.vue                    # home
│  │  │  │  ├─ auth/
│  │  │  │  │  ├─ login.vue
│  │  │  │  │  └─ register.vue
│  │  │  │  ├─ books/
│  │  │  │  │  ├─ index.vue                 # catalogo
│  │  │  │  │  └─ [id].vue                  # dettaglio/lettura
│  │  │  │  ├─ comics/index.vue             # fumetti
│  │  │  │  ├─ comics/[id].vue              # dettaglio/lettura fumetto
│  │  │  │  ├─ manga/index.vue              # manga
│  │  │  │  ├─ manga/[id].vue               # dettaglio/lettura manga
│  │  │  │  ├─ newspapers/index.vue         # giornali
│  │  │  │  ├─ newspapers/[id].vue          # dettaglio/lettura giornale
│  │  │  │  └─ profile/index.vue
│  │  │  ├─ components/
│  │  │  │  ├─ ItemCarousel.vue            # carousel oggetti con NuxtLink
│  │  │  │  ├─ ItemCard.vue
│  │  │  │  └─ ObjectCard.vue              # singolo item nella carousel
│  │  │  ├─ layouts/
│  │  │  ├─ middleware/
│  │  │  │  └─ auth.ts                      # protezione pagine interne
│  │  │  ├─ composables/
│  │  │  ├─ plugins/
│  │  │  └─ assets/
│  │  ├─ server/
│  │  │  ├─ api/                            # controller (MVC)
│  │  │  │  ├─ auth/
│  │  │  │  │  ├─ login.post.ts
│  │  │  │  │  ├─ register.post.ts
│  │  │  │  │  ├─ logout.post.ts
│  │  │  │  │  └─ mfa-verify.post.ts
│  │  │  │  ├─ books/
│  │  │  │  │  ├─ index.get.ts
│  │  │  │  │  ├─ [id].get.ts
│  │  │  │  │  └─ [id]/comments.post.ts
│  │  │  │  ├─ library/
│  │  │  │  │  ├─ index.get.ts
│  │  │  │  │  └─ save.post.ts
│  │  │  │  └─ purchases/
│  │  │  │     └─ checkout.post.ts          # transazione + lock
│  │  │  ├─ services/                       # logica applicativa
│  │  │  ├─ repositories/                   # query DB prepared statement
│  │  │  ├─ utils/
│  │  │  │  ├─ validation.ts                # sanitizzazione input
│  │  │  │  └─ session.ts                   # cookie/sessioni
│  │  │  └─ db/
│  │  │     └─ client.ts
│  │  ├─ nuxt.config.ts
│  │  ├─ tailwind.config.ts
│  │  └─ package.json
│  │
│  └─ realtime/                             # Socket.IO server separato
│     ├─ src/
│     │  ├─ index.ts
│     │  ├─ events/
│     │  │  ├─ comments.ts
│     │  │  └─ notifications.ts
│     │  └─ middleware/
│     │     └─ auth-socket.ts
│     └─ package.json
│
├─ packages/
│  └─ shared/
│     ├─ types/
│     ├─ constants/
│     └─ validators/
│
├─ database/
│  ├─ er/
│  │  └─ er-diagram.png                     # diagramma ER da consegnare
│  ├─ schema/
│  │  └─ schema.sql
│  ├─ migrations/
│  └─ seed/
│
├─ docs/
│  ├─ requisiti-checklist.md                # mappa voci prof -> implementazione
│  ├─ api-integrations.md                   # Google API / email / pagamenti
│  └─ presentazione.md
│
├─ .nvmrc                                   # versione Node uguale casa/scuola
├─ package.json
└─ README.md

Navigazione oggetti / pagina dinamica:
- `app/components/ItemCarousel.vue`: carousel che mostra gli item e usa `<NuxtLink :to="...">` per andare alla pagina dell'oggetto.
- `app/pages/books/[id].vue`: pagina dinamica per il dettaglio del libro con `params.id`.
- `app/pages/comics/[id].vue`, `app/pages/manga/[id].vue`, `app/pages/newspapers/[id].vue`: pagine dinamiche per le altre tipologie.
- `app/server/api/books/[id].get.ts`: endpoint per leggere i dati del singolo oggetto.
- Comportamento atteso: click su item carousel → route dinamica `/${tipo}/${id}` → dettaglio oggetto.

Note operative (MVP):
- PostBack (richiesta prof): endpoint login/register in server routes con validazione e risposta sulla stessa rotta.
- Sessioni/Cookie: cookie HttpOnly + SameSite, salvataggio sessione in tabella sessions.
- Prepared statement: tutte le query passano da repositories.
- Transazioni + FOR UPDATE: in checkout acquisto libro.
- AJAX/WebSocket: CRUD via fetch/AJAX, commenti live via Socket.IO.
- MFA: codice OTP via email in login.

API
Categoria	API	                                Costo                       Serve per	                                        Attenzione
Libri	    Open Library	                    Gratis	                    ricerca, autori, ISBN, copertine	                rate limit, non pensata come backend ad alto traffico
Libri	    Gutendex	                        Gratis	                    testo completo dei libri di Project Gutenberg	    solo pubblico dominio; per uso lungo meglio self-host
Libri	    Google Books	                    Gratis con API key	        catalogo più ampio, preview, copertine	            non dà il testo completo
Manga	    Jikan (MyAnimeList)	                Gratis	                    ricerca, rating pubblici, popularity	            niente chapters (solo metadata)
Manga	    Comick Source API	                Gratis	                    capitoli e fonti di lettura	                    API non ufficiale; stabilita variabile
Manga       MangaDex                            Gratis                      url delle immagini e capitoli (fondamentale)
Manga       AniList                             Gratis                      GraphQL, relazioni, tags, media, ranking, recommendations
News	    GDELT	                            Gratis e open data	        news globali, ricerca multilingua, aggregazione	    meglio per aggregare che per avere il testo completo di una testata
News	    The Guardian Open Platform	        Gratis con developer key	article text, tag, sezioni	                        non commerciale, 500 richieste al giorno
News	    NewsAPI	                            Gratis solo per dev/test	headline e ricerca rapida articoli	                100 richieste al giorno, ritardo di 24 ore, niente full article content

API per i libri:
documentazione:
        Open Library: https://openlibrary.org/swagger/docs#/
        Gutendex: https://gutendex.com/
        Google Books: https://developers.google.com/books/docs/v1/using
progetto DEV/TEST — Google Books va bene anche senza testo completo

API per i manga:
documentazione:
    Jikan (MyAnimeList): https://docs.api.jikan.moe/ oppure AniList: https://docs.anilist.co/guide/introduction
    MangaDex: https://api.mangadex.org/docs/

API per news/giornali:
documentazione:
        The Guardian Open Platform: https://open-platform.theguardian.com/
        GDELT (analytics, big data): https://www.gdeltproject.org/data.html

*** (non utilizzate)
- Comick Source API (Manga chapters)
    - Base URL: https://comick-source-api.notaspider.dev
    - Come chiamare: POST /api/search (query, source), POST /api/chapters (url, source)
        Esempio: POST https://comick-source-api.notaspider.dev/api/search {"query":"solo leveling","source":"mangapark"}
    - Forma risposta: `results[]` con `id`, `title`, `url`, `coverImage`, `latestChapter`; `chapters[]` con `number`, `title`, `url`.
    - Endpoints utili: GET /api/sources, GET /api/frontpage, GET /api/health
    - Errori comuni: 400 Bad Request, 404 Not Found, 429 Too Many Requests, 500 Server Error.
    - Note: aggrega fonti esterne; usare mapping per titolo/URL; adatto per dev/test (stabilita variabile).

***

---
Dettagli pratici per integrazione (richiami, formato risposta, errori)

- Jikan (Manga metadata/ratings)
    - Base URL: https://api.jikan.moe/v4
    - Come chiamare: GET /manga?q=<term> oppure GET /manga/{id}
        Esempio: https://api.jikan.moe/v4/manga?q=one%20piece
    - Forma risposta: JSON con `data` (array) o record singolo; campi tipici `mal_id`, `title`, `images`, `score`, `favorites`, `synopsis`.
        Esempio ridotto: {"data":[{"mal_id":1,"title":"...","score":8.7}]}
    - Errori comuni: 404 Not Found, 429 Too Many Requests, 500 Server Error.
    - Note: API non ufficiale di MyAnimeList; ottima per rating pubblici e metadata; non fornisce capitoli.


- MangaDex (Per i chapters e url immagini)
- Base Url https://api.mangadex.org/
- ricerca Manga GET /manga                                              esempio: https://api.mangadex.org/manga?title=one piece
Risposta: 
{
  "data": [
    {
      "id": "32d76dba-...",
      "attributes": {
        "title": {
          "en": "One Piece"
        }
      }
    }
  ]
}
- Ottieni i capitoli GET /manga/{mangaId}/feed                          esempio: https://api.mangadex.org/manga/32d76dba-.../feed
Risposta: 
{
  "data": [
    {
      "id": "chapter-id-123",
      "attributes": {
        "chapter": "1",
        "title": "Romance Dawn"
      }
    }
  ]
}
- Ottieni il server immagini (@Home) GET /at-home/server/{chapterId}    esempio: https://api.mangadex.org/at-home/server/chapter-id-123
Risposta tipica:
{
  "baseUrl": "https://uploads.mangadex.org",
  "chapter": {
    "hash": "abcd1234",
    "data": [
      "1.jpg",
      "2.jpg",
      "3.jpg"
    ],
    "dataSaver": [
      "1.jpg",
      "2.jpg",
      "3.jpg"
    ]
  }
}
- Costruire URL immagini    Qualità normale: {baseUrl}/data/{hash}/{filename}                   esempio: https://uploads.mangadex.org/data/abcd1234/1.jpg
                            Data Saver (compressione): {baseUrl}/data-saver/{hash}/{filename}   esempio: https://uploads.mangadex.org/data-saver/abcd1234/1.jpg

Problemi comuni:
1. Troppe immagini insieme, NON fare Promise.all(images.map(load)) per 50 pagine.
I CDN: rate-limitano, chiudono connessioni, alcune immagini falliscono
Meglio: lazy loading, caricare progressivamente, virtual scrolling

2. Hotlink / CORS
Alcuni browser: bloccano immagini, timeoutano, Meglio usare backend proxy.

- Open Library (Books metadata)
    - Base URL: https://openlibrary.org
    - Come chiamare: GET /search.json?q=<term>
        Esempio: https://openlibrary.org/search.json?q=harry+potter
    - Forma risposta: JSON con `numFound` e `docs` (array). Ogni doc contiene title, author_name, isbn, edition_key, ecc.
        Esempio: {"numFound":123,"docs":[{"title":"...","author_name":["..."],"isbn":["..."]}, ...]}
    - Errori comuni: 429 Rate Limit, 500 Server Error.
    - Note: non richiede chiave; usare `User-Agent` con email per limiti migliori. Default ~1 req/sec, 3 rps se identificati.

- Google Books
    - Base URL: https://www.googleapis.com/books/v1
    - Come chiamare: GET /volumes?q=<term>&key=<API_KEY>
        Esempio: https://www.googleapis.com/books/v1/volumes?q=tolkien&key=YOUR_KEY
    - Forma risposta: JSON con `kind`, `totalItems`, `items`[]. Ogni item contiene `id` e `volumeInfo` (title, authors, imageLinks, accessInfo...)
        Esempio: {"kind":"books#volumes","totalItems":2,"items":[{"id":"...","volumeInfo":{"title":"..."}}]}
    - Errori comuni: 400/401/403 (API key mancante o non valida), 429 Quota exceeded, 500 Server Error.
    - Note: richiede API key per produzione; utile per cover/previews/accessInfo.

- Gutendex (Project Gutenberg metadata)
    - Base URL: https://gutendex.com/books
    - Come chiamare: GET /books?search=<term>&page=<n>
        Esempio: https://gutendex.com/books?search=frankenstein
    - Forma risposta: {"count":<n>,"next":"...","previous":null,"results":[{id,title,authors,formats,...}]}
    - Errori comuni: 404 (id non trovato), 500.
    - Note: ottimo per public-domain texts; nessuna auth; per carico elevato meglio self-hostare.

- The Guardian Open Platform (News)
    - Base URL: https://content.guardianapis.com
    - Come chiamare: GET /search?q=<term>&api-key=<KEY>&show-fields=thumbnail,trailText,body
        Esempio: https://content.guardianapis.com/search?q=climate&api-key=YOUR_KEY&show-fields=trailText,thumbnail
    - Forma risposta: {"response":{"status":"ok","results":[{"id":"...","webTitle":"...","webUrl":"...","fields":{...}}]}}
    - Errori comuni: 401/403 (chiave), 429 (quota giornaliera), 500.
    - Note: richiede registrazione per ottenere API key; quota tipica per developer ≈500 req/day (richiedere aumento se necessario).

- GDELT (analytics/news dataset)
    - Tipologia: dataset + API di sintesi; uso tipico via BigQuery o download dei dump.
    - Per UI: preferibile usare Summary APIs o un proprio pipeline; non è pensato per fetch di singoli articoli.
    - Errori/attenzioni: dataset molto grande, latenza e complessità di query; non adatto come unica sorgente per articoli completi.

Consigli integrazione (pratici):
- Implementare un proxy server-side (Nuxt server routes) che normalizzi risposte e mantenga le chiavi API.
- Cache preferita: Redis. TTL suggeriti: search = 1h, dettaglio risorsa = 24h, news = 30m.
- Rate-limiter upstream (es. Bottleneck): reservoir 60/min, minTime ~334ms; gestire 429 con backoff/escalation.
- Usare ETag / If-None-Match per evitare traffico inutile; servire 304 Not Modified quando possibile.
- Tradurre errori upstream in payload client-friendly (es. mettere header X-Upstream-Status, Retry-After).

---
PROFILI 
ruoli (role):
    user (o null): è un utente normale, può leggere, mettere like, commentare ...
    author: sono gli autori, possono pubblicare opere, modificare le proprie e fare tutto ciò che un utente normale può fare. Avranno il tag certificato accanto quando fanno un commento. Per diventare un'autore, devi fare la richiesta ai manager o agli admin
    manager: sono gli "helper" dell'admin, tipo la segreteria. ognuno potra avere permessi diversi (scelti dall'admin), tipo accettare richieste di creazione autori, modificare opere (magari soltanto i tag, come età minima, copertina, il genere...)
    admin: è l'admin, può modificare tutti gli utenti (la pagina admin sara ovviamente accessibilem solo all'admin), crearli, eliminarli... Può anche modificare le opere a piacimento. 

---
OPERE
le opere create dagli autori interni al sito saranno quelle inserite nel database. le opere prese dalle API e quelle del database sono di tipo diverso. le opere interne al database sono "Opere interne", avranno una sezione diversa. 

---
PAGINA SINGOLA DEGLI ELEMENTI

Obiettivo della pagina:
- click su un item della carousel -> route dinamica `/${tipo}/${id}`
- lettura dei dati tramite endpoint server-side Nuxt
- rendering della scheda oggetto con contenuti diversi in base al tipo

Flusso comune:
1. Il componente `ItemCarousel.vue` invia l'`id` e il `tipo` del contenuto.
2. La pagina dinamica `[id].vue` legge `route.params.id`.
3. La pagina chiama la server route corretta, per esempio `/api/books/[id]` o l'equivalente per manga/comics/news.
4. La server route normalizza la risposta dell'API esterna o del DB in un formato unico.
5. La pagina mostra cover, metadati, descrizione, azioni e contenuti correlati.

Oggetto normalizzato comune:
- `id`: identificativo interno o esterno
- `type`: `book`, `comic`, `manga`, `news`
- `source`: `db`, `openlibrary`, `googlebooks`, `gutendex`, `jikan`, `comick`, `guardian`, `gdelt`
- `title`
- `subtitle`
- `authors`
- `description`
- `coverUrl`
- `contentUrl`
- `language`
- `publishedAt`
- `tags`
- `rating`
- `price`
- `isFree`
- `isSaved`
- `isPurchased`
- `commentsCount`

STRUTTURA API E PAGINA

1. LIBRI

API preferite: Open Library, Google Books, Gutendex.

Open Library
- Request: `GET /search.json?q=<term>` oppure `GET /works/<key>.json`
- Input principale: testo di ricerca, `key` dell'opera, eventuale pagina
- Response: `numFound`, `docs[]` o record singolo con titolo, autori, ISBN, copertina, soggetto, anno
- Uso nel sito: ricerca catalogo, metadati, copertine, link alla scheda libro
- Pagina dettaglio: hero con copertina, autore, anno, generi, sinossi, pulsanti salva/acquista/leggi, sezione libri simili

Google Books
- Request: `GET /volumes?q=<term>&key=<API_KEY>` oppure `GET /volumes/<id>`
- Input principale: query di ricerca, id volume, filtri facoltativi
- Response: `totalItems`, `items[]`, `volumeInfo`, `accessInfo`, `imageLinks`
- Uso nel sito: catalogo ricco, preview, copertine, stato di accesso al volume
- Pagina dettaglio: blocco preview, metadati completi, badge `preview`, pulsante apri anteprima, commenti, salvataggio

Gutendex
- Request: `GET /books?search=<term>&page=<n>` oppure `GET /books/<id>`
- Input principale: termine di ricerca, pagina, id libro
- Response: `count`, `next`, `previous`, `results[]`, con titolo, autori, formati, testo in pubblico dominio
- Uso nel sito: libri gratuiti e leggibili integralmente
- Pagina dettaglio: evidenza del testo completo, pulsante leggi ora, formati disponibili, download, capitoli o sezioni

Server route suggerita per i libri
- `/api/books/index.get.ts`: ricerca e lista catalogo
- `/api/books/[id].get.ts`: dettaglio singolo libro
- `/api/books/[id]/comments.post.ts`: nuovo commento

2. MANGA

API consigliate: Jikan o AniList (metadata, rating, recommendations...) e MangaDex (Capitoli, url Immagini)
I manga sono divisi in capitoli, quindi la pagina principale mostra metadata e rating da Jikan e la lista capitoli da Comick; il reader puo aprire il capitolo interno oppure rimandare alla fonte esterna.

Nota mapping Jikan/AniList <-> MangaDex
- Match per titolo principale + titoli alternativi; salvare entrambi gli `external_id` (Jikan `mal_id` + URL/ID Comick) in catalog_items.

Server route suggerita per i manga
- `/api/manga/index.get.ts`: ricerca e lista catalogo
- `/api/manga/[id].get.ts`: dettaglio singolo manga
- `/api/manga/[id]/comments.post.ts`: nuovo commento

3. COMICS

Fonte prevista: database interno o API esterna da decidere in base alla disponibilità.

Request
- Se interno: `GET /api/comics/index.get.ts` con filtri `page`, `genre`, `search`
- Se esterno: stessa logica di proxy e normalizzazione del resto del progetto

Response
- array con `title`, `coverUrl`, `chapters`, `language`, `description`, `status`, `type`

Uso nel sito
- catalogo a griglia con copertine
- pagina dettaglio orientata alla lettura di immagini o capitoli

Pagina dettaglio comics
- cover e titolo in alto
- lettore immagini o capitoli sequenziali
- box info con autore, genere, stato, numero tavole
- azioni: salva, compra, commenta, continua la lettura

Server route suggerita per i comics
- `/api/comics/index.get.ts`
- `/api/comics/[id].get.ts`
- `/api/comics/[id]/comments.post.ts`

4. GIORNALI E NEWS

API possibili: The Guardian Open Platform, GDELT.

The Guardian Open Platform
- Request: `GET /search?q=<term>&api-key=<KEY>&show-fields=thumbnail,trailText,body`
- Input principale: parola chiave, sezione, data, paginazione
- Response: `response.status`, `response.results[]`, con titolo, URL, estratto, immagine, corpo articolo
- Uso nel sito: news leggibili, ricerca per argomento, articoli completi dove permesso
- Pagina dettaglio: titolo, fonte, data, immagine, sommario, corpo articolo, link originale, salvataggio

GDELT
- Request: query su endpoint di sintesi o pipeline interna, non come singolo articolo classico
- Input principale: argomento, lingua, finestra temporale, area geografica
- Response: dataset aggregato, eventi, articoli correlati, trend e fonte
- Uso nel sito: sezione giornali più orientata a raccolta e trend che a lettura articolo per articolo
- Pagina dettaglio: riepilogo, correlazioni, articoli collegati, fonte di provenienza

Server route suggerita per i giornali
- `/api/news/index.get.ts`
- `/api/news/[id].get.ts`
- `/api/news/[id]/comments.post.ts`

STRUTTURA UI DELLA PAGINA DETTAGLIO

Sezioni comuni:
- header con breadcrumb e categoria
- hero con cover/immagine principale
- colonna sinistra con preview o lettore
- colonna destra con metadata e azioni
- tab o sezioni per descrizione, capitoli, commenti, correlati

Comportamento responsive:
- desktop: layout a due colonne
- mobile: hero in alto, azioni sotto, lettore a tutta larghezza
- la carousel deve sempre rimandare alla pagina dettaglio dello stesso tipo di contenuto

Componenti utili:
- `ObjectHeader.vue`: titolo, autore, badge, rating
- `ObjectActions.vue`: salva, acquista, leggi, condividi
- `ObjectMetadata.vue`: anno, genere, lingua, stato, source
- `ObjectReader.vue`: area lettura o preview
- `RelatedItems.vue`: contenuti simili
- `CommentsPanel.vue`: commenti live via Socket.IO

Regola di progetto:
- tutte le API esterne vengono passate da server routes Nuxt
- la pagina non parla mai direttamente con l'API esterna
- ogni risposta viene normalizzata nello stesso formato, così la UI resta uguale per libri, manga, comics e news

---
Inserimento oggetti da API nel database, Like, commenti, liste...
Quando un utente apre la pagina di un oggetto, che sia Libro, Manga o News, questa viene inserita nel database coem catalog_items, source sarà ovviamente API ed external_provider da quale API viene presa ed external_id l'ID dell'oggetto dall'API. grazie a questo, i like e commenti (saranno tutti interni al sito) possono essere messi sia agli oggetti presi dalle API che a quelli interni al sito. 

FLUSSO APPLICATIVO (OGGETTI API + LIKE/COMMENTI)

1) Apertura pagina oggetto
- Input pagina: type + external_id (se arriva da API esterna) oppure item_id interno.
- Server route:
    a) Se source=api: fetch API esterna.
    b) Normalizza risposta nel formato comune.
    c) Upsert catalog_items (chiave unica: type + source + external_provider + external_id).
    d) Upsert catalog_item_translations per lingua (title/description).
    e) Upsert item_media per cover/immagini.
    f) views_count = views_count + 1.
    g) Carica stato utente: isLiked, isSaved, isPurchased, canLike, canComment.
    h) Risposta: item_id interno + oggetto normalizzato + flags.

2) Like (toggle)
- UI click sul cuore.
- Se non loggato: login modal o redirect.
- Backend: insert/delete su item_likes.
- Se like = true: aggiunge anche in lista "Preferiti" (user_list_items).
- Risposta: liked, likesCount, saved.

3) Commenti
- UI invio commento.
- Backend: validazione + sanitizzazione input.
- Permessi: canComment se reading_progress.percentage >= soglia (default 10%, configurabile).
- Inserimento in comments.
- Se Socket.IO attivo: broadcast nuovo commento alla pagina.

UI LIKE
- Bottone cuore con stato: off (contorno) / on (pieno).
- Tooltip: "Aggiungi ai preferiti" / "Rimuovi dai preferiti".
- Contatore like vicino al bottone.
- Stato loading durante la request, update ottimistico con rollback su errore.

UI COMMENTI
- Header con conteggio commenti.
- Form con textarea + bottone "Invia".
- Lista commenti: avatar, username, badge ruolo, data, testo, rating (se usato).
- Stato vuoto: "Nessun commento ancora".
- Paginazione o "Carica altri".

- - - - - - - - - - - - - - - - -


Dettagli tecnici per le "liste" (requisiti funzionali e API)

- **DB (tabelle principali):**
  - `user_lists` (id, user_id, title, description, cover_image, is_public BOOLEAN, tags JSON/CSV, created_at, updated_at)
  - `user_list_items` (id, list_id, item_type, external_provider, external_id, added_at, position INT)
  - Indici: `user_lists(user_id)`, `user_list_items(list_id)`, `user_list_items(external_provider, external_id)`

- **API server routes (server/api):**
  - `GET /api/lists` — lista delle liste dell'utente (con filtro `public=true` per liste pubbliche)
  - `POST /api/lists` — crea nuova lista (body: title, description?, cover_image?, is_public?, tags[])
  - `GET /api/lists/[id]` — dettaglio lista + paginazione items
  - `PUT /api/lists/[id]` — aggiorna metadata lista (title, cover, privacy, tags)
  - `DELETE /api/lists/[id]` — elimina lista (solo owner)
  - `POST /api/lists/[id]/items` — aggiungi item a lista (body: item_type, external_provider, external_id)
  - `DELETE /api/lists/[id]/items/[itemId]` — rimuovi item da lista
  - `POST /api/lists/[id]/reorder` — riordina item (body: array di itemId/position)

- **Comportamento UI / flusso utente:**
  - Modal/inline card per scegliere la lista di destinazione quando si clicca "salva" su un oggetto.
  - Possibilità di creare una nuova lista dalla stessa card.
  - Ogni utente ha una lista `Preferiti` creata automaticamente; quando un utente mette like a un oggetto, opzionalmente viene aggiunto anche ai Preferiti (configurabile).
  - Nella pagina "Le mie liste" mostrare: titolo, cover, numero elementi, visibilità (pubblica/privata), tag principali.
  - Dettaglio lista: paginazione degli item, filtri per `type` (book/manga/comic/news), ordinamenti (posizione, data aggiunta, titolo).

- **Validazioni e limiti:**
  - `tags`: stringa max 20 caratteri; massimo N tag per lista (es. 10).
  - `title` lista: max 100 caratteri.
  - Limite consigliato: max 50 liste per utente, max 1000 items per lista (configurabile).
  - Solo il proprietario può modificare/riordinare/eliminare la lista; le liste pubbliche sono leggibili da altri utenti.

- **Privacy e permessi:**
  - Impostazione globale in profilo per default delle nuove liste (public/private).
  - Le singole liste sovrascrivono l'impostazione globale.
  - Utenti non confermati: possono creare e salvare in liste, ma non possono mettere Like o Commentare (coerente con le regole già indicate).

- **Integrazione con catalog_items:**
  - Quando un oggetto API viene visualizzato e viene upsertato in `catalog_items`, non duplicare l'inserimento in `user_list_items`: usare `external_provider + external_id` per riferirsi all'item.

- **API client-side (UI examples):**
  - `POST /api/lists` { title, is_public }
  - `POST /api/lists/123/items` { item_type: 'manga', external_provider: 'anilist', external_id: '98765' }

- **Extra utilità:**
  - Endpoint per esportare/ condividere lista pubblica (es. `/api/lists/[id]/export` — JSON o link pubblico).
  - Copertura con test: endpoint CRUD liste + operazioni items + permessi.
 
---
Implementazioni completate (2026-05-22):
- Search Users (UI + API): sezione ricerca utenti aggiunta e risultati rimandati alla pagina profilo.
- Preferenze profilo: `image_size`, `account_public`, `lists_public_by_default` (API GET/POST + UI settings).
- Campo `bio`: aggiunta la proprietà `bio` al profilo utente e visualizzazione in pagina pubblica.
- Richieste artista: endpoint per richiesta artista e interfaccia admin per accettare/rifiutare; approvazione promuove a `artist`.
- Salvataggi e liste: API `lists/contains`, indicatori `Salvato` in `ItemCard.vue`, miglioramenti modale con copertine/tag delle liste.
- Badge artista nei commenti: `CommentsPanel.vue` mostra badge per ruolo `artist`.
- Pagina profilo pubblica: `pages/users/[id].vue` implementata per profili pubblici e liste pubbliche.

Queste voci sono state rimosse da `modificheDaFare.txt` secondo la regola #1.